# Local development — copy to .env.local

# API URL (leave as /api/v1 to use Vite proxy in dev)
VITE_API_URL=/api/v1

# Socket.io — local backend
VITE_SOCKET_URL=http://localhost:5000

# App name
VITE_APP_NAME=PNMC Management System
